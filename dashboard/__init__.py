"""
CUSTOM-SOC dashboard package.

The dashboard is a presentation layer only.

It must consume trusted metrics from the reporting service and must not
query the database directly or recreate SOC aggregation logic.
"""